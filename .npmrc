# Bleeding-edge majors (Next 16 / React 19 / eslint-config-next 16) have a few strict
# peer ranges that npm's default resolver rejects. We install with legacy peer resolution
# (locally and on Vercel) so `npm install` succeeds; the versions are pinned in package.json.
legacy-peer-deps=true
